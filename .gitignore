# macOS
.DS_Store
._*

# Editors
.vscode/
.idea/

# Duplicate of profile.jpeg at 7x the size — the page uses the .jpeg.
# Delete the file locally if you don't want it, or remove this line to publish it.
assets/profile.png

# scratch: safe to delete
_to_delete/